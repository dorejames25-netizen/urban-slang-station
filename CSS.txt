:root {
  --font-heading: 'Orbitron', sans-serif;
  --font-body: 'Rajdhani', sans-serif;

  /* DEFAULT MOOD: CYBER PLASMA */
  --bg-primary: #0a0612;
  --bg-panel: rgba(20, 15, 38, 0.65);
  --accent-plasma: #ff007f;
  --accent-secondary: #00f0ff;
  --border-glow: rgba(0, 240, 255, 0.4);
  --text-primary: #ffffff;
  --text-muted: #8e9bb0;
  --card-bg: #140c28;
}

/* THEME VARIATIONS */
[data-mood="PINK_CHAOS"] {
  --bg-primary: #12030a;
  --bg-panel: rgba(40, 10, 25, 0.7);
  --accent-plasma: #ff0055;
  --accent-secondary: #ff77bc;
  --border-glow: rgba(255, 0, 85, 0.5);
  --card-bg: #2b0818;
}

[data-mood="GREEN_PARTY"] {
  --bg-primary: #02120a;
  --bg-panel: rgba(10, 35, 20, 0.7);
  --accent-plasma: #00ff66;
  --accent-secondary: #aaff00;
  --border-glow: rgba(0, 255, 102, 0.5);
  --card-bg: #072917;
}

[data-mood="PURPLE_MYSTERY"] {
  --bg-primary: #090314;
  --bg-panel: rgba(25, 12, 45, 0.7);
  --accent-plasma: #9900ff;
  --accent-secondary: #00d9ff;
  --border-glow: rgba(153, 0, 255, 0.5);
  --card-bg: #1c0a36;
}

[data-mood="BLACK_FILTH"] {
  --bg-primary: #050505;
  --bg-panel: rgba(20, 20, 20, 0.8);
  --accent-plasma: #ff3300;
  --accent-secondary: #666666;
  --border-glow: rgba(255, 51, 0, 0.5);
  --card-bg: #111111;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  background-color: var(--bg-primary);
  color: var(--text-primary);
  font-family: var(--font-body);
  font-size: 18px;
  min-height: 100vh;
  overflow-x: hidden;
  transition: background 0.5s ease;
}

/* GLASS PANEL styling */
.glass-panel {
  background: var(--bg-panel);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid var(--border-glow);
  border-radius: 16px;
  padding: 24px;
  box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.5);
}

.app-container {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  padding: 20px;
  max-width: 1400px;
  margin: 0 auto;
}

/* HEADER NAV */
.app-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px 24px;
  background: var(--bg-panel);
  border-radius: 16px;
  border: 1px solid var(--border-glow);
  margin-bottom: 24px;
}

.brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.brand-logo {
  font-family: var(--font-heading);
  font-weight: 900;
  font-size: 24px;
  color: var(--bg-primary);
  background: linear-gradient(135deg, var(--accent-plasma), var(--accent-secondary));
  padding: 8px 12px;
  border-radius: 8px;
}

.brand-text h1 {
  font-family: var(--font-heading);
  font-size: 20px;
  letter-spacing: 2px;
}

.brand-text .sub {
  font-size: 12px;
  color: var(--accent-secondary);
  letter-spacing: 1px;
}

.main-nav {
  display: flex;
  gap: 8px;
}

.nav-btn {
  background: transparent;
  border: 1px solid transparent;
  color: var(--text-muted);
  padding: 10px 18px;
  border-radius: 8px;
  font-family: var(--font-heading);
  font-size: 13px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.nav-btn:hover, .nav-btn.active {
  color: var(--text-primary);
  border-color: var(--accent-secondary);
  background: rgba(255, 255, 255, 0.05);
  box-shadow: 0 0 12px var(--border-glow);
}

/* PLASMA BUTTON STYLING */
.plasma-button-wrapper {
  margin-top: 20px;
}

.plasma-button {
  position: relative;
  width: 220px;
  height: 220px;
  border-radius: 50%;
  border: none;
  background: transparent;
  cursor: pointer;
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 0 auto;
}

.plasma-orb {
  position: absolute;
  width: 100%;
  height: 100%;
  border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, var(--accent-secondary), var(--accent-plasma));
  box-shadow: 0 0 40px var(--accent-plasma), inset 0 0 20px rgba(255, 255, 255, 0.8);
  animation: plasmaPulse 3s infinite alternate ease-in-out;
}

.plasma-button:hover .plasma-orb {
  transform: scale(1.05);
  filter: brightness(1.2);
}

.plasma-label {
  position: relative;
  z-index: 2;
  font-family: var(--font-heading);
  font-weight: 900;
  font-size: 16px;
  color: #fff;
  text-shadow: 0 0 10px #000;
  letter-spacing: 2px;
}

@keyframes plasmaPulse {
  0% { box-shadow: 0 0 30px var(--accent-plasma), inset 0 0 15px rgba(255,255,255,0.6); }
  100% { box-shadow: 0 0 60px var(--accent-secondary), inset 0 0 30px rgba(255,255,255,0.9); }
}

/* PAGE VIEWS */
.page-view {
  display: none;
}

.page-view.active {
  display: block;
  animation: fadeIn 0.4s ease-in-out;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* INTAKE FORM STYLES */
.intake-form {
  display: flex;
  flex-direction: column;
  gap: 20px;
  margin-top: 20px;
}

.form-group {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.form-group label {
  font-family: var(--font-heading);
  font-size: 14px;
  color: var(--accent-secondary);
}

.form-group input, .form-group select, .form-group textarea {
  background: rgba(0,0,0,0.4);
  border: 1px solid var(--border-glow);
  padding: 12px;
  border-radius: 8px;
  color: #fff;
  font-family: var(--font-body);
  font-size: 16px;
}

.swear-bar-container {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 12px;
}

.swear-level-btn {
  background: rgba(0, 0, 0, 0.4);
  border: 1px solid var(--border-glow);
  padding: 16px;
  border-radius: 12px;
  cursor: pointer;
  display: flex;
  flex-direction: column;
  gap: 4px;
  transition: all 0.3s ease;
}

.swear-level-btn.active {
  border-color: var(--accent-plasma);
  background: rgba(255, 0, 127, 0.15);
  box-shadow: 0 0 15px var(--accent-plasma);
}

/* MATRIX GRID */
.matrix-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
}

.matrix-grid-display {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 16px;
}

.card-item {
  background: var(--card-bg);
  border: 1px solid var(--border-glow);
  border-radius: 12px;
  padding: 16px;
  min-height: 200px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  position: relative;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card-item:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 25px var(--border-glow);
}

.card-number {
  font-family: var(--font-heading);
  font-size: 12px;
  color: var(--accent-secondary);
}

.card-text {
  font-size: 16px;
  line-height: 1.4;
  margin: 12px 0;
}

.card-badge {
  align-self: flex-start;
  font-size: 11px;
  background: var(--accent-plasma);
  padding: 2px 8px;
  border-radius: 4px;
  text-transform: uppercase;
}

/* BUTTONS */
.action-btn {
  background: linear-gradient(135deg, var(--accent-plasma), var(--accent-secondary));
  border: none;
  padding: 14px 28px;
  color: #fff;
  font-family: var(--font-heading);
  font-weight: 700;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.3s ease;
}

.action-btn:hover {
  filter: brightness(1.2);
  box-shadow: 0 0 20px var(--accent-plasma);
}

/* PRINT CSS OVERRIDES */
@media print {
  body {
    background: #fff !important;
    color: #000 !important;
  }
  .app-header, .main-nav, .print-controls {
    display: none !important;
  }
  .print-sheet-preview {
    display: grid !important;
    grid-template-columns: repeat(3, 3.5in);
    gap: 0.1in;
  }
  .card-item {
    border: 1px solid #000 !important;
    color: #000 !important;
    background: #fff !important;
    page-break-inside: avoid;
  }
}